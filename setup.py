from setuptools import setup, find_packages

setup(
    name="h3pandas",
    version=0.1,
    author="dahn",
    author_email="dahnjahn@gmail.com",
    packages=find_packages(),
    setup_requires=[],
    install_requires=[],
    python_requires=">=3.7"
)
